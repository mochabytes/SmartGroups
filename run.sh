# full app will run if user just does ./run.sh in their terminal

echo "Starting up Smart Groups!"

# check if python is installed
# check if npm is installed / node is installed
# check if pip is installed
# check if pipenv is installed

# check if venv already exists

# install backend dependencies using venv creating smartgroups_venv
# install frontend dependencies using npm

# start backend
# start frontend

echo ""
echo "Smart Groups is now running."
echo "To stop the app, press Ctrl+C in the terminal."
echo ""
echo "To access the app, go to http://localhost:3000 in your browser."
echo ""
echo "To access the backend, go to http://localhost:5000/api/v1/docs in your browser."
echo ""

# cleanup function

wait





