import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <div className="header-container">
            <h1 className="header-title">
                SmartGroups
            </h1>
            <p className="header-subtitle">
                Simple group formation with constraints
            </p>
        </div>
    );
};

export default Header; 